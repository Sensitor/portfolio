__pycache__/
*.pyc

# Local persistence — saved portfolios and snapshots. Personal financial data;
# never committed. Override the location with SENSITOR_DB_PATH.
sensitor_data.db
*.db-journal

# Node — the mobile client's dependencies are installed, never committed.
node_modules/
mobile/node_modules/
package-lock.json
